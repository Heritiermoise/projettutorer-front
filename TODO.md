# TODO

## Step plan (to implement task)
1. Clarify the intended change requested by the user task "salut" (where it should appear / what to change).
2. Search relevant files (HomePage/App/layout) for existing greeting text.
3. Update the appropriate component(s) to include "salut".
4. Run the dev/build command to verify.

